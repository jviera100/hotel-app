# .env.example - Plantilla de variables de entorno
# Copia este archivo a .env y rellena tus propios valores.

#----Conexión a la base de datos PostgreSQL local (para Docker Compose)-------
DB_DIALECT=postgres
DB_HOST=db # Nombre del servicio de la base de datos en docker-compose.yml
DB_DATABASE=reservas_hotel
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123456
SECRET_KEY=hotel

#----Credenciales de Gmail para Nodemailer (opcional)--------------
# Si no usas esta funcionalidad, puedes dejar estos campos vacíos o eliminarlos.
EMAIL_USER=tu_email@gmail.com
EMAIL_PASS=tu_contraseña_de_aplicacion_gmail

#----Conexión a base de datos remota en Neon (ejemplo, no usada por Docker Compose por defecto)------
# Estas variables son para una conexión remota y no son necesarias para la configuración local con Docker Compose.
# PGHOST=ep-rough-darkness-a5zfcvzy-pooler.us-east-2.aws.neon.tech
# PGDATABASE=skatepark
# PGUSER=neondb_owner
# PGPASSWORD=wL0iMuOA5jBY