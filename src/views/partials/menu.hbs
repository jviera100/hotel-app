<!-- menu.hbs -->
<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
  <div class="container">
    <a class="navbar-brand logo" href="/"><img class="logo-navbar" src="/img/javascript.svg" alt="logo-navbar"></a>
      {{!-- <a id="titulo-navbar"
             class="nav-link text-light"
             aria-current="page"
             href="./M3-28-prueba-fundamentos-de-programacion-en-javascript.pdf" target="_blank"
             >doct% -</a
     > --}}
     {{!-- <a id="titulo-navbar"
             class="nav-link text-light"
             aria-current="page"
             href="./M4-22-prueba-programacion-avanzada-en-javascript.pdf" target="_blank"
             >- nada</a
     > --}}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
       <ul class="navbar-nav ms-auto">
        <!-- Botón "Home" -->
        {{#if showhomeButton}}
        <li class="nav-item">
          <a class="nav-link home-btn {{#if (eq currentPage 'home')}}active{{/if}}" href="/">Home<i class="fa-solid fa-house ms-2"></i></a>
        </li>
        {{/if}}
        <!-- Botón "Registrarse" -->
        {{#if showRegisterButton}}  
          <li class="nav-item">
            <a class="nav-link register-btn {{#if (eq currentPage 'registro')}}active{{/if}}" href="/registro">Check in</a>  
          </li>
        {{/if}}
        <!-- Botón "Iniciar Sesión" -->
        {{#if showLoginButton}}
          <li class="nav-item">
            <a class="nav-link login-btn {{#if (eq currentPage 'login')}}active{{/if}}" href="/login">Log in</a>
          </li>
        {{/if}}        
        <!-- Botón "Contacto" -->
        {{#if showContactButton}}
        <li class="nav-item">
          <a class="nav-link contacto-btn {{#if (eq currentPage 'contacto')}}active{{/if}}" href="/contacto">Contact</a>
        </li>
        {{/if}}        
        <!-- botones para usuarios autenticados -->
        {{#if isAuthenticated}}        
        <li class="nav-item">
          <!-- Botón "saludo usuario" -->
          {{#if showGreetingButton}}
          <li class="nav-item">
            <span class="nav-link saludo-btn">Hello, {{currentUserName}}</span>
          </li>
          {{/if}}                    
          <!-- Botón "Ver Perfil" -->
          {{#if showProfileButton}}
          <li class="nav-item">
            <a class="nav-link perfil-btn" href="/perfil/{{currentUserEmail}}">View profile</a>
          </li>
          {{/if}}          
          <!-- Botón "Cerrar Sesión" -->
          {{#if showLogoutButton}}
          <li class="nav-item">
            <a class="nav-link logout-btn" href="/logout">Log out</a>
          </li>
          {{/if}}
        {{/if}}
      </ul>
    </div>
  </div>
</nav>